package com.sbaygildin.taskpuller.data.local

import javax.inject.Inject

//class InboxNotesRepositoryImpl @Inject constructor(
//    private val dao: ImageMetaDao,
//) {
//    suspend fun getImagesForDataset(dataset: String): List<ImageMeta> =
//        dao.getImagesForDataset(dataset)
//
//    suspend fun clearDataset(dataset: String) = dao.deleteByDataset(dataset)
//    suspend fun insertImages(images: List<ImageMeta>) = dao.insertAll(images)
//}